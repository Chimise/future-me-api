export enum MessageType {
    Email = 'email',
    Audio = 'audio',
    Video = 'video',
}

export enum MessageStatus {
    Waiting = 'waiting',
    Scheduled = 'scheduled',
    Sent = 'sent',
    Failed = 'failed',
}
